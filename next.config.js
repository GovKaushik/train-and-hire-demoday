/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,           // temp for debug
  swcMinify: false,                 // temp for clearer stacks
  experimental: {},                 // keep empty for now
};
module.exports = nextConfig;